{
  "name": "PresentacionSimposio",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "live-server --port=8080 --open=index.html",
    "dev": "live-server"
  },
  "private": true,
  "dependencies": {
    "reveal.js": "^5.2.1"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
